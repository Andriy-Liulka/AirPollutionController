@model SongModel


<h2>My current song is "@Model.Name"</h2>

<h2>----------------------------------</h2>

@for(int i=0; i < Model.Notes.Count; i++)
{
	<span>@Model.Notes[i]</span>
	if (i % 10 == 0)
	{
		<br>
	}
}